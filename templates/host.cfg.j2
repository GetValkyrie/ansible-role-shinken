# See: http://shinken.readthedocs.org/en/latest/08_configobjects/host.html

define host {
  host_name      {{ item.value.hostname }}
  address        {{ item.value.address | default(item.value.hostname) }}
  check_command  {{ check_command | default('check_ping') }}
}
